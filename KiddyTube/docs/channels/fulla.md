# Fulla / فلة — RETIRED

- **Channel id:** `fulla` (removed in seed **v15**)
- **Status:** Retired — dropped on catalog seed upgrade

Fulla was removed from the default home grid per product direction. Existing installs drop the channel when they merge seed v15+. Parents who still want it can add videos manually, but it is no longer shipped.
